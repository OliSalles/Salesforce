public with sharing class DataMassController {
    @AuraEnabled
    public static String sendDataToServer(String data) {
        // Logic to handle data (e.g., insert records)
        // Example:
        Map<String, Object> dataMap = (Map<String, Object>) JSON.deserializeUntyped(data);
        // Do something with dataMap
        return 'Success';
    }
}